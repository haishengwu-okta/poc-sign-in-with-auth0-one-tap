<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Atko ID</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <div class="container">
            <h1 class="header">Akto ID</h1>
            <% if (isLogin) { %>
                <a href="/logout">Logout</a>
                <pre style="font-size: 20px;"><%= profile %></pre>
            <% } %>
            <% if (!isLogin) { %>
                <a href="/login">login</a>
                <!-- <a href="/email">email</a> -->
            <% } %>

        </div>
    </body>
</html>
